pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.py"
      run: uvx ruff check .

    - name: fix
      glob: "*.py"
      run: uvx ruff check --fix .

    - name: format
      glob: "*.py"
      run: uvx ruff format .
